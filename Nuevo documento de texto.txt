1. install composer
2. command:  composer create-project laravel/laravel example-app
3.configurar base de datos en archivo .env
4.  php artisan make:model Cliente -mc
 a- 
  public function up(): void
    {
        Schema::create('cliente', function (Blueprint $table) {
            $table->increments('id');
            $table->string('nombre',120);
            $table->string('apellido',120);
            $table->string('direccion',200);
            $table->string('telefono',20);
            $table->string('email',100);
           
            $table->timestamps();
        });
    }

5.php artisan migrate (esto crea en la base de datos la tabla que se creo en archivo database->migations->create_clientes_table.php)


